
let data_tree = {}

function createMoveTreNode(data, parent_node_name) {
  let node = new Object(data, parent_node_name) {
    data = data,
    name = parent_node_name,
    left = null,
    right = null
  }
  return node
}

function simulateGameMove(game_board, turn_indicator) {

}

function addNodeToTree(data, parent_node, direction) {

  if (direction === 0) {
    let new_node = createMoveTreNode(data, parent_node.name)
  } else if (direction === 1) {
    let new_node = createMoveTreNode(data, parent_node.name)
  }

  data_tree[new]


}

